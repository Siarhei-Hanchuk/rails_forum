require 'spec_helper'

describe User do
	#before :each do
    #@user = User.create()
  #end

  #it "should not be valid without a first_name" do
  #	@user = User.create()
  #  @user.roles=["user"]<<:admin
  #  @user.should be_is :admin
  #end
  
end
