<h1>Editing user</h1>

<%= form_for(@user) do |f| %>

  <%=image_tag @user.avatar.url, :width=>100 %>
  <br>
  <%= f.file_field :avatar %>

  <div class="actions">
    <%= f.submit 'Update avatar'%>
  </div>

<% end %>

<%= form_for(@user) do |f| %>

<!--   <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %> -->

  <div class="field">
    <%= f.label :pass %><br />
    <%= f.text_field :pass %>
  </div>

  <div class="field">
    <%= f.label :pass_confirmation %><br />
    <%= f.text_field :pass_confirmation %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Update password' %>
  </div>
<% end %>

<%= form_for(@user) do |f| %>

   <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Update email' %>
  </div>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
